{
  "name": "expensesplit",
  "version": "1.0.0",
  "description": "ExpenseSplit is a modern, modular SaaS platform for tracking shared expenses, built with a strictly-typed Clean Architecture.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky",
    "lint-staged": "lint-staged"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx}": [
      "npm --prefix frontend run lint",
      "npm --prefix frontend run format"
    ],
    "backend/**/*.ts": [
      "npm --prefix backend run lint",
      "npm --prefix backend run format"
    ]
  }
}